# Supabase — Fase 2. Enquanto vazio, o app roda 100% com o serviço mock em memória.
# Como obter: Supabase → Project Settings → API (ver supabase/README.md).
# A chave publishable é pública (segurança vem da RLS); NUNCA use a secret/service_role aqui.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Legado (opcional): se seu projeto ainda usar a chave antiga "anon", pode usar esta no lugar.
# VITE_SUPABASE_ANON_KEY=
